import LoginForm from "./components/LoginForm";

/**
 * Login Page
 *
 * 사용자 로그인 페이지
 * - 이메일/비밀번호 로그인 폼
 * - 회원가입 링크
 */
const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
